import { Routes } from '@angular/router';
import { MobileQuoteComponent } from '../components/mobile-quote/mobile-quote.component';

export const routes: Routes = [
  {
    path: 'mobile-quote',
    component: MobileQuoteComponent
  }
];
