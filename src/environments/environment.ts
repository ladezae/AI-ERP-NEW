// This file is a placeholder.
// In a standard Angular CLI project, you would store environment-specific
// configuration here, such as API keys. For this applet environment,
// we are instructed to use `GEMINI_API_KEY` directly in the service.
export const environment = {
  production: false,
};
